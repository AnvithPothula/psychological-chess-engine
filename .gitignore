# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.mypy_cache/

# Secrets -- never commit
.env
.env.*
!.env.example

# macOS
.DS_Store

# Mined DPO data (reproducible via mine_overnight.sh)
build/*.jsonl
build/distill_corpus.npz

# Trained checkpoints (reproducible via distill.py / train_dpo.py).
# Maia weights (models/*.pb.gz) are inputs and stay tracked.
*.pth

# Run artifacts
build/*.log
build/session.json
build/games/

# Maia-2 checkpoint cache (273MB, auto-downloaded)
maia2_models/
